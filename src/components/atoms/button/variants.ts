import { COLORS } from '../../../constants/colors';

export const BLACK_BUTTON = {
  backgroundColor: `${COLORS.PEARL_BLACK}`,
  color: `${COLORS.WHITE}`,
};

export const WHITE_BUTTON = {
  backgroundColor: `${COLORS.WHITE}`,
  color: `${COLORS.PEARL_BLACK}`,
};

export const GREEN_BUTTON = {
  backgroundColor: `${COLORS.GREEN}`,
  color: `${COLORS.WHITE}`,
};

export const GREEN_BUTTON_ALT = {
  backgroundColor: `${COLORS.GREEN}`,
  color: `${COLORS.GOLD}`,
};

export const ROOM_BUTTON = {
  backgroundColor: `${COLORS.PEARL_GREY}`,
  borderRadius: '50px',
  color: `${COLORS.PEARL_BLACK}`,
  fontWeight: `${600}`,
  padding: '8px 24px',
};

export const WHATSAPP_BUTTON = {
  backgroundColor: `${COLORS.PEARL_BLACK}`,
  fontSize: '0.5rem',
  margin: '0.25rem',
  padding: '0.25rem 1rem',
};

export const CREAM_BUTTON = {
  backgroundColor: `${COLORS.CREAM}`,
  color: `${COLORS.PEARL_BLACK}`,
};

export const GOLD_BUTTON = {
  backgroundColor: `${COLORS.GOLD}`,
  borderRadius: '50px',
  color: `${COLORS.PEARL_BLACK}`,
  fontWeight: `${600}`,
  margin: '0 16px',
};

export const BLACK_GOLD_BUTTON = {
  backgroundColor: `${COLORS.PEARL_BLACK}`,
  borderRadius: '50px',
  color: `${COLORS.GOLD}`,
  fontWeight: `${600}`,
  margin: '0 16px',
};