import styled from '@emotion/styled';
import { COLORS } from '../../../constants/colors';

export const DisclaimerContainerStyled = styled.div`
  width: 100%;
  padding: 4px;
  background-color: ${COLORS.RED};
`;
